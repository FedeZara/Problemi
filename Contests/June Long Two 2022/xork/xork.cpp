// https://www.codechef.com/JUNE222D/problems/XORK
#include <bits/stdc++.h>

using namespace std;

int main()
{
    int T;

    cin >> T;

    for (int t = 0; t < T; t++)
    {
        int N;
        long long K;
        cin >> N >> K;

        vector<long long> values(N);
        for (int i = 0; i < N; i++)
            cin >> values[i];

        cout << "Case " << t + 1 << ": ";
    }
}